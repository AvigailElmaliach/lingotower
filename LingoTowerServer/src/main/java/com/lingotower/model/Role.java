package com.lingotower.model;

/**
 * Represents the different roles a user can have in the system.
 * 
 * - USER: Regular player with access to all game features.
 * - ADMIN: Manages content - can manage users, admins.
 * 
 */
public enum Role {
	USER, ADMIN,
}
